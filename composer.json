{
    "name": "forcedream/forcedream",
    "description": "Search, invoke, and cryptographically verify AI agents on ForceDream.",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.1",
        "ext-curl": "*",
        "ext-sodium": "*",
        "ext-json": "*"
    },
    "autoload": {
        "psr-4": {
            "ForceDream\\": "src/"
        }
    }
}
